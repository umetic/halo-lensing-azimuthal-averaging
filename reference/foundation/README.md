# Foundation reference targets

This directory contains compact validation targets for the cosmology, NFW, and triaxial-projection foundation layer.

The targets support tests of distances, source weights, NFW projected functions, mass normalization, projection identities, and selected historical anchors. They are validation references, not inputs copied into generated science products.
