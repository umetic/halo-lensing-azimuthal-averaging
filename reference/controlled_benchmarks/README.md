# Controlled-benchmark reference targets

This directory contains compact validation targets for the deterministic
single-halo benchmarks used by the paper.  These values are not generated by
`azlens.controlled_benchmarks`; they were transcribed from the source-audited
specification and the validated Figure 2 frozen input tables.

The full generated benchmark tables belong under `outputs/` and are not
committed.
