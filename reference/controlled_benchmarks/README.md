# Controlled-benchmark reference targets

This directory contains compact reference values for the deterministic single-halo benchmark layer. The targets cover the controlled calculations associated with Figures 1--2 and Appendix D.

The values are validation targets. They are not used as inputs copied into newly generated products.
